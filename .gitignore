*.iml
.gradle
/local.properties
/.idea
/.kotlin
.DS_Store
/build
build
.dart_tool
.kotlin
# Mason resolves bricks from `mason.yaml` (relative `path:` entries). The lock /
# cache files below are machine-local absolute paths regenerated by `mason get`
# after clone — committing them pins every consumer to THIS machine's checkout.
.mason/
mason-lock.json