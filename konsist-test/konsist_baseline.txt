# Konsist baseline — pre-existing architecture violations tolerated until their cleanup task.
#
# Format:  <RuleId> <fully-qualified declaration>            // TODO(task_N): <what to do>
# Blank lines and lines starting with '#' are ignored; an inline '//' comment is stripped.
# Removing a line re-arms the corresponding rule for that declaration — do that as
# part of the referenced task, never here.
#
# Only rules that are ENFORCED need entries. Enforced now: K2, K3, K4, K5, K6, K7, K8, K9
# (K2/K3/K7 enabled in Task 9 and K6/K9 enabled in Task 11 all passed clean — no baseline
# entries required). Still deferred / report-only: K1 (@Ignore, consults no baseline).
#
# The baseline is currently EMPTY:
# - Task 13 deleted the five wallet-domain features (authentication / myWallet / transactions /
#   trends / splash), so their 34 K4 entries and their K5 ViewModel entries no longer name
#   anything that exists.
# - Task 13 also converted the three surviving `*ViewModel`s (SettingsViewModel, ProfileViewModel,
#   ScannerViewModel) to `MviViewModel<State, Action, Event>`, clearing the last K5 entries.
